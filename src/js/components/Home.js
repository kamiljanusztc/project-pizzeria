
class Home {
  constructor(element) {
    const thisHome = this;

    thisHome.render(element);
    thisHome.initWidgets();
  }

  render() {
    const thisHome = this;

    coonst generatedHTML = templates.
  }

  initWidgets() {
    const thisHome = this;
  }
}

export default Home;
